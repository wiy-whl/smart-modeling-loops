# 建模方案产物模板

## 元数据

```yaml
version: "1.0.0"
status: "draft"          # draft | review | approved | archived
author: "ai-assistant"
reviewer: null
created_at: "{{YYYY-MM-DD}}"
updated_at: "{{YYYY-MM-DD}}"
source_requirement: "inputs/raw/{{需求文件名}}.md"
```

## 业务背景

<!-- 用 2-3 句话描述本方案服务的业务场景 -->

## 实体关系概览

```mermaid
erDiagram
    {{实体A}} ||--o{ {{实体B}} : "{{关系描述}}"
```

## 实体定义

### {{实体名称}}

**业务描述**：<!-- 该实体代表的业务对象 -->

**YAML 定义**：

```yaml
{{实体名_snake}}:
  description: "{{实体业务描述}}"
  table_name: "{{表名}}"
  fields:
    - name: "id"
      type: "uuid"
      primary_key: true
      description: "主键"
    - name: "created_at"
      type: "timestamp"
      auto_generate: true
      description: "创建时间"
    - name: "updated_at"
      type: "timestamp"
      auto_generate: true
      description: "更新时间"
    # 业务字段...
  indexes:
    - fields: ["{{字段名}}"]
      type: "btree"
      unique: false
  validations:
    - field: "{{字段名}}"
      rule: "required"
    # 更多验证规则...
```

## 关系定义

| 源实体 | 目标实体 | 关系类型 | 基数 | 外键字段 | 描述 |
|--------|---------|---------|------|---------|------|
| {{A}} | {{B}} | belongs_to | N:1 | {{B}}_id | {{说明}} |

## 字段映射表

<!-- 从业务术语到模型字段的详细映射 -->

| 业务术语 | 模型字段 | 实体 | 数据类型 | 必填 | 说明 |
|---------|---------|------|---------|------|------|
| {{业务词}} | {{snake_field}} | {{实体}} | {{type}} | 是/否 | {{说明}} |

## 设计决策记录

| 决策 | 选项 | 选择 | 理由 |
|------|------|------|------|
| {{决策点}} | {{方案A}}/{{方案B}} | {{选择}} | {{业务原因}} |

## 已知限制与待确认项

- [ ] {{待确认项1}}
- [ ] {{待确认项2}}
